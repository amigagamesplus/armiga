#!/bin/sh
# S29alsa — Restaurar niveles ALSA desde rootfs
case "$1" in
    start)
        alsactl -f /etc/alsa/asound.state restore
        ;;
    stop|restart)
        ;;
    *)
        echo "Usage: $0 {start|stop|restart}"
        exit 1
        ;;
esac
