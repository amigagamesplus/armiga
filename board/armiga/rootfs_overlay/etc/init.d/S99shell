#!/bin/sh
# =============================================================================
# S99shell — Fase 1: login shell con info del sistema
# En fases posteriores se reemplazará por S99amiberry
# =============================================================================
case "$1" in
    start)
        echo ""
        echo "============================================"
        echo "  Armiga — Fase 1 — Base System"
        echo "============================================"
        echo ""
        echo "  SSH:  root@$(hostname) (puerto 22)"
        echo "  Pass: armiga"
        echo ""
        echo "============================================"
        ;;
    stop)
        ;;
    *)
        ;;
esac
